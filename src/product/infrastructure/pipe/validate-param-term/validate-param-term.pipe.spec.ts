import { ValidateParamTermPipe } from './validate-param-term.pipe';

describe('ValidateParamTermPipePipe', () => {
  it('should be defined', () => {
    expect(new ValidateParamTermPipe()).toBeDefined();
  });
});
